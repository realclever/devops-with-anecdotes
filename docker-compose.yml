version: '3.5'

services:

  anecdotes:
    build: .
    ports:
      - 3000:3000
    volumes:
      - ./:/usr/src/app
      - node_modules:/usr/src/app/node_modules
    command: npm start

volumes:
  node_modules:
